query slider($id: Int!) {
    slider(id: $id) {
        id
        title
    		slides {
          id
          title
          image_url
          content
          content_position
          content_appear_animation
        }
    }
}
